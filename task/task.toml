artifacts = ["/app/output.json"]

[task]
name = "dynamo/warehouse-robot-diagnostics"
description = "Analyze warehouse robot operations and generate a fleet diagnostic report."

[metadata]
category = "Data Science and Reporting"
subcategory = "Operational analytics"

model_tested = "GPT-5.4"
agent_tested = "Terminus-2"

task_objective = ["transform","validate"]

artifact_type = [
"dataset_or_tabular_file",
"generated_output_artifact",
"single_script_or_program"
]

expert_time_estimate_hours = 1

difficulty_explanation = "Requires event reconstruction, ordered filtering, joins, fleet analytics and deterministic JSON generation."

solution_explanation = "Merge multiple datasets, truncate event history after failures, compute operational metrics and serialize the report."

verification_explanation = "Verifier checks schema, statistics and calculated fleet metrics."

[verifier]
timeout_sec = 300

[agent]
timeout_sec = 600

[environment]
build_timeout_sec = 600
cpus = 1
memory_mb = 2048
storage_mb = 10240
allow_internet = true
mcp_servers = []